* all results use bias correction
* SplitSampleEstimate.jl uses n=400 as base, and fits larger samples using it

Takeaways:
both bias correction and splitting sample work. Splitting sample does not get all the
way to MLE, but helps. 

In a sub-branch, will go on to explore TCN
