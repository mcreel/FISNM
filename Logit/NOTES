To explore:
* Slightly more complicated net for larger sample sizes
* different optimizers, learning rates?


to replicate:

* start Julia with julia --proj -t auto
* include("estimate.jl")
* include("MakePlots.jl")
* MakePlots("working")

