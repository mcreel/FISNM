# reads in the testing data (n X mcreps)
# and estimates MA2 model for each replication
# results are in mcreps x 2 csv file

# adjust and run for each sample size used

open data100.csv --quiet
list vlist = dataset
matrix results = zeros(1000,2)

loop foreach i vlist
    arma 0 2; $i --nc --quiet
    results[i,] = $coeff'
endloop

store "~/git/FISNM/MA2/MLE-n100.csv" --matrix=results    
