set echo off
set messages off

open data_100.csv --quiet
list vlist = dataset
matrix results = zeros(5000,2)

loop foreach i vlist
    arma 0 2; $i --nc --quiet --opg #--lbfgs
    results[i,] = $coeff'
endloop

store "~/Desktop/MLE100.csv" --matrix=results    
