set echo off
set messages off

open ../Testing/data_3200.csv --quiet
list vlist = dataset
matrix results = zeros(5000,2)

loop foreach i vlist
    arma 0 2; $i --nc --quiet --lbfgs
    results[i,] = $coeff'
endloop

store "~/git/FISNM/MA2/MLE3200.csv" --matrix=results    
